import { readFileSync } from 'fs';
import _ from 'lodash';

export function solveX_1(inputPath: string): number {
    const parsedInput = parseInput(inputPath);

    return 0;
}

export function solveX_2(inputPath: string): number {
    const parsedInput = parseInput(inputPath);

    return 0;
}

const parseInput = (inputPath: string): string[][] => {
    const file = readFileSync(inputPath, 'utf-8');

    return [];
}